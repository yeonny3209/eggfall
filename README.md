# EGGFALL

드래곤 오픈월드 MMO — 기획서 v0.2 구현

> 모든 용은 서로 다른 스킬을 갖고, 세계에는 단 열두 개의 신성이 있으며, 그 열둘은 언제나 사냥당할 수 있다.

## 현재 상태: **Phase 0 · 1 · 2 완료**

**§2 핵심 루프가 끝까지 돕니다.**

```
탐색 → 회수(E) → 운반(이동 −25%) → 둥지 귀환 → 흡수 → 성장 → 외형 변화
```

- **Phase 0 (비행)** — 처음엔 관성·실속·스태미나가 있는 항공기식 모델이었지만, 조작이 너무 어렵다는
  피드백을 받아 **훨씬 쉬운 스펙테이터 캠 모델**로 바꿨습니다: 마우스가 보는 방향이 곧 날아가는
  방향이고, 스태미나·실속·상승기류는 없습니다.
- **Phase 1 (알)** — 자연 둥지 64곳에서 알이 스폰되고, 주워서 홈 둥지로 가져가 흡수하면 성장합니다.
- **Phase 2 (멀티플레이)** — Colyseus 권위 서버 + 클라이언트 예측/보정 + 원격 보간. 최대 8인.
  **서버가 없으면 자동으로 싱글플레이가 됩니다** — GitHub Pages 는 정적 호스팅이라 서버를 띄울 수 없기 때문입니다.

전투는 아직 없습니다 (Phase 3).

## 멀티플레이 실행

```bash
npm run server   # 터미널 1 — Colyseus 서버 (localhost:2567)
npm run dev      # 터미널 2 — 클라이언트
```

localhost 에서는 자동으로 로컬 서버에 붙습니다. 다른 서버를 쓰려면:

| 방법 | 예시 |
|---|---|
| 쿼리 파라미터 | `?server=ws://192.168.0.5:2567` |
| 빌드 환경변수 | `VITE_SERVER_URL=wss://내서버.com npm run build` |

서버에 못 붙으면 조용히 싱글플레이로 떨어집니다. HUD 좌하단에 접속 상태가 표시됩니다.

## 실행

```bash
npm install
npm run dev
```

| 명령 | 내용 |
|---|---|
| `npm run dev` | 개발 서버 (localhost:5273) |
| `npm run check` | 타입 검사 |
| `npm test` | 테스트 90개 (비행 21 + 알 스폰 25 + 성장 25 + 넷코드 19) |
| `npm run server` | Colyseus 게임 서버 (localhost:2567) |
| `npm run build` | 프로덕션 빌드 |

실서버 스모크 테스트: `npm run server` 후 `npx tsx scripts/smoke.ts`
(두 클라이언트를 붙여 동기화·입력 처리·줍기 중재·퇴장을 확인합니다)

## 조작

| 입력 | 행동 |
|---|---|
| 마우스 | 시점 회전 — **보는 방향이 곧 비행 방향** (클릭으로 활성화) |
| `W` / `S` | 보는 방향 기준 전진 / 후진 |
| `A` / `D` | 보는 방향 기준 좌우 이동 |
| `Space` | 누르는 동안 상승 |
| `Shift` | 누르는 동안 하강 |
| `E` | 알 줍기 / 내려놓기 |

스태미나·실속·상승기류는 없습니다. 무제한으로, 마음먹은 대로 날 수 있습니다.

## 구현된 것

**비행 모델** (`src/flight/simulate.ts` — Three.js 의존 없는 순수 함수)

- "스펙테이터 캠" 모델: 마우스가 정한 목표 시점을 실제 자세가 뒤쫓는다.
  성장 단계별 `turnPenalty` 가 그 추적 속도를 늦춰 **큰 용일수록 시점 반응이 굼떠진다** (§6.3 불변 규칙 유지).
- 목표 속도로 부드럽게 가감속한다 (순간 정지/출발이 아니다).
- A/D 는 시선의 피치와 무관하게 항상 수평으로만 이동 — 위를 올려다봐도 강제로 뜨지 않는다.
- 실제 지형과 충돌해 착지/이륙한다.

**알 스폰** (`src/egg/spawn.ts` — 역시 Three.js 의존 없는 순수 함수)

- 자연 둥지 64곳은 **시드에서 결정론적으로** 생성된다. 위치를 저장할 필요가 없고, 서버·클라가 같은 월드를 본다.
- 한 둥지에 알은 최대 하나. 주우면 5~15분 뒤 리스폰한다 (§7.1).
- 등급은 가중 랜덤(일반 1000 : 고급 380 : 희귀 110 : 영웅 26). **신성 알은 자연 둥지에서 나오지 않는다** — 시즌당 12회 각성 전용 (§5.1).
- 등급이 유전 질량과 특성 수를 결정한다. 특성은 흡수 시 스킬 발현 풀의 재료가 된다 (§4.4).
- 난수 상태를 `SpawnerState` 에 담아 그대로 직렬화 가능 — 오프라인 후 재접속해도 경과 시간만큼 알아서 채워진다.

**운반 · 흡수 · 성장** (`src/player/progress.ts` — 순수 함수)

- 알은 **한 번에 하나만** 들 수 있고, 드는 순간 이동속도가 −25% 된다 (§2 게임의 중심축).
  운반 중에는 드래곤 앞발에 알이 실제로 보인다.
- 월드 중앙 **홈 둥지**로 돌아가 1.8초 채널링하면 흡수된다. 둥지는 알을 들고 있을 때 더 밝게 빛난다.
- 흡수하면 세 가지가 동시에 일어난다: 유전 질량 누적 → 단계 판정 / 속성 친화도 누적 → 외형 변화 /
  특성이 발현 풀에 축적 → Phase 4 스킬 생성기의 재료.
- **성장은 오직 유전 질량 총합으로만 결정된다** (§6.1). 임계값 `[0, 50, 180, 500, 1200, 2600]`.
- 친화도에 따라 순혈(70%+) / 이종(40·40) / 잡종으로 갈린다 (§3.5).

**외형 변화** (§6.2 — 저장하지 않고 결정론적으로 계산)

- 몸집이 단계별 배율로 커지고, **등 가시가 단계 수만큼 늘어난다** — 숫자 UI 없이 성장이 실루엣으로 읽힌다.
- 비늘 색은 누적 친화도의 가중 혼합. 뇌풍 위주로 먹으면 보랏빛, 반석 위주면 흙빛이 된다.
- 단계가 오르면 선회 반응도 함께 둔해진다 (§6.3 불변 규칙).

**멀티플레이** (`src/net/`, `server/` — §11 Phase 2, §13.6 에 따라 렌더링과 완전히 분리)

- **권위 서버**: 위치·알 스폰·줍기·흡수를 전부 서버가 판정한다. 서버가 권위를 갖는 가장 큰 이유는
  치트 방지가 아니라 **알 하나를 두 명이 동시에 주울 수 없어야** 하기 때문이다 (§2 의 전제).
- **예측(prediction)**: 내 입력은 서버 응답을 기다리지 않고 즉시 적용된다. 100ms 왕복에서도 조작이 즉각적이다.
- **보정(reconciliation)**: 서버 상태가 오면 그 시점으로 되감고 아직 확인 안 된 입력만 재생한다.
  어긋난 만큼은 화면에서 서서히 흡수해 튀지 않는다.
  **서버와 클라이언트가 같은 `stepFlight` 를 돌리기 때문에 성립한다** — Phase 0 에서 시뮬레이션을
  렌더링과 분리해둔 투자가 여기서 회수된다.
- **보간(interpolation)**: 남의 드래곤은 ~125ms 과거로 그린다. 외삽은 하지 않는다 —
  맞추려다 틀리면 캐릭터가 벽을 뚫고 되돌아오는데, 살짝 늦는 것보다 훨씬 나쁘다.
- **서버 검증**: `dt` 상한(스피드핵 차단), 줍기 사거리, 중복/역행 시퀀스, 입력 큐 상한.
- 원격 플레이어는 같은 절차적 생성기로 그려지고 이름표가 붙는다.
  **알을 든 플레이어는 이름표 색이 바뀐다** — §2 "알을 든 순간 곧 표적이 된다".

**월드** (`src/world/`)

- 8km × 8km 절차 지형. **렌더 메시 · 충돌 · 그림자가 모두 같은 `terrainHeight()` 를 쓴다** (`terrain.ts`).
- 랜드마크 첨탑 240개 — 속도감의 근거.
- 고층 경계에 구름층, 그라디언트 하늘, 지면 그림자.
- 알은 속성 색으로 빛나고 위아래로 떠다니며, **희귀 이상은 빛기둥**을 세워 멀리서도 보인다 (`eggs.ts`).

**HUD** (`src/ui/hud.ts`)

- 속도 · 고도 · 층 표시, 착지 알림, 고속 스트릭 효과
- **알 파인더** — 가장 가까운 알의 등급·속성·거리와 기수 기준 방향 화살표, 고도차(▲▼), 주변 개수

## 구조

```
src/
  types.ts              기획서 §10 데이터 모델 — "AI 의 기억" (§13.2)
  data/
    balance.json        모든 수치가 여기 하나에 (§13.4)
    divines.json        12신성 = 데이터, 코드 아님 (§13.5)
  flight/
    simulate.ts         비행 물리 — 순수 함수, Three.js 없음
    input.ts            키보드 → FlightInput
    camera.ts           추적 카메라
  egg/
    spawn.ts            알 스폰 — 순수 함수, Three.js 없음
  player/
    progress.ts         운반 · 흡수 · 성장 · 친화도 — 순수 함수
  net/
    protocol.ts         클라이언트·서버 공유 계약 (§13.2)
    schema.ts           Colyseus 동기화 상태
    prediction.ts       예측 · 보정 · 보간 — 순수 함수, Colyseus 의존 없음
    client.ts           접속 · 원격 추적 (실패 시 싱글플레이 폴백)
server/
  index.ts              서버 진입점
  WorldRoom.ts          권위 방 — 모든 판정
  world/
    terrain.ts          지형 높이 단일 진실 공급원
    scene.ts            월드 구성
    atmosphere.ts       하늘 · 구름 · 그림자
    dragon.ts           절차적 드래곤 메시 (단계별 가시 · 친화도 색)
    eggs.ts             알 렌더링 · 빛기둥
    homeNest.ts         홈 둥지 — 핵심 루프의 종착점
    remotes.ts          원격 플레이어 렌더링 · 이름표
  ui/hud.ts
tests/
  flight.test.ts        비행 시뮬레이션 테스트 21개
  spawn.test.ts         알 스폰 테스트 25개
  progress.test.ts      성장 · 운반 · 흡수 테스트 25개
  prediction.test.ts    예측 · 보정 · 보간 테스트 19개
scripts/smoke.ts        실서버 2인 스모크 테스트
```

### 설계 원칙 (§13 지침 준수)

1. **타입 우선** — `src/types.ts` 가 계약. 여기 없는 개념은 존재하지 않는 것으로 취급.
2. **순수 함수 분리** — `simulate.ts` / `terrain.ts` 는 렌더링·네트워크와 무관. Phase 2 에서 서버가 그대로 재사용한다.
3. **시뮬레이션 ↔ 렌더링 분리** — 시뮬레이션은 고정 timestep(1/60), 렌더링은 rAF. 프레임률이 물리 결과를 바꾸지 않는다.
4. **모든 수치는 `balance.json`** — 코드 수정 없이 튜닝 가능.
5. **12신성은 데이터** — `divines.json`. 하드코딩하면 유지보수 불가.

### 개발용 콘솔 핸들

개발 빌드에서 `window.__eggfall` 로 상태를 들여다볼 수 있습니다.

```js
__eggfall.pause()      // 시뮬레이션 정지
__eggfall.pump(3)      // 3초분 수동 진행
__eggfall.state        // FlightState
```

## 다음 단계 (기획서 §11)

| Phase | 내용 |
|---|---|
| ~~**1**~~ | ~~알 스폰 → 운반 → 흡수 → 성장 → 외형 변화~~ ✅ **완료** |
| ~~**2**~~ | ~~멀티플레이 (Colyseus), 예측/보정, 8인~~ ✅ **완료** |
| **3** | 전투 + 속성 상태이상 6종 |
| **4** | 반응 시스템 + 절차적 스킬 생성기 + 자동 명명 |
| **5** | 바이옴 6종, 유성우, 3층 고도, 둥지 |
| **6** | 신성 알 12개, 각성 카운터, 왕관의 무게, 핵 강탈 |

`SeasonState` 와 `counters` 타입은 Phase 6 을 위해 §13 권고대로 미리 `types.ts` 에 넣어두었습니다.

## 기술

Three.js · TypeScript · Vite · Colyseus 0.16.

물리 엔진(Rapier)은 지형 충돌이 해석적 함수로 충분해 아직 도입하지 않았고, 오브젝트 충돌이
필요한 Phase 3(전투)에서 추가합니다.

### 서버 배포

GitHub Pages 는 정적 호스팅이라 게임 서버를 띄울 수 없습니다. 멀티플레이를 공개하려면
Node 프로세스를 돌릴 수 있는 곳(Render / Railway / Fly.io / Colyseus Cloud)에 `server/` 를
배포하고, 클라이언트를 `VITE_SERVER_URL=wss://주소` 로 빌드하면 됩니다.
설정하지 않으면 배포된 게임은 싱글플레이로 동작합니다.
